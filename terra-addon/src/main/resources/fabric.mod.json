{
  "schemaVersion": 1,
  "id": "bubbleschunkgen",
  "version": "${version}",
  "name": "BubblesOnChunkGen",
  "description": "Terra addon that activates bubble columns in CHIMERA-generated rivers. Also loads as a Fabric mod to register the water-flow mixin.",
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "server": [
      "com.bubbleschunkgen.terra.platform.FabricEntry"
    ]
  },
  "mixins": [
    "bubbleschunkgen.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.18.0",
    "minecraft": ">=26.1"
  }
}
